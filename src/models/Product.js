export default class Product {
  constructor(id, name, description, image, price, stock, weight) {
    this.id = id
    this.name = name
    this.description = description
    this.image = image
    this.price = price
    this.stock = stock
    this.weight = weight
  }
}
